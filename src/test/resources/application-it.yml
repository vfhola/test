spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:tc:postgresql:16.2
    username: user
    password: password


scheduler:
  queue:
    cron: ${TEST_CRON:0 0 0 31 12 6}